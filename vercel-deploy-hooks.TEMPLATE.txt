# vercel-deploy-hooks.TEMPLATE.txt (tracked in git — no secrets here)
#
# deploy.bat REQUIRES this: without two hook URLs it will FAIL at step 5 (after git push).
# Git alone often builds only one Vercel project; hooks guarantee BOTH xevora + xevora-app.
#
# This file is NOT used directly. On first deploy, deploy.bat creates deploy-hooks.txt (gitignored).
# Put real hook URLs in deploy-hooks.txt (or any name below). First match wins:
#   deploy-hooks.txt                 <- default; created from this template if missing
#   .vercel-deploy-hooks.txt
#   vercel-deploy-hooks.txt
#   vercel-deploy-hooks.example.txt
#   vercel-deploy-hooks.example
#
# Vercel: each project (xevora + xevora-app) -> Settings -> Git -> Deploy Hooks
# -> Create hook -> Branch: main -> Copy the HTTPS URL.
#
# Two REQUIRED lines (different URLs — one per Vercel project):
#   Line 1: Deploy Hook from project **xevora** (marketing site)
#   Line 2: Deploy Hook from project **xevora-app** (Next.js app)  <- must NOT be the same URL as line 1
#
# One https:// URL per line (no quotes). Lines starting with # are ignored.
#
# https://api.vercel.com/v1/integrations/deploy/prj_xxxx/yyyyyyyy
# https://api.vercel.com/v1/integrations/deploy/prj_zzzz/wwwwwwww
