# Copy to ONE of these in the repo root (same folder as deploy.bat):
#   .vercel-deploy-hooks.txt   (leading dot - in Notepad use "Save as type: All files (*.*)")
#   vercel-deploy-hooks.txt     (no dot - also works; both names are gitignored)
#
# Vercel: each project (xevora + xevora-app) -> Settings -> Git -> Deploy Hooks
# -> Create hook -> Branch: main -> Copy the HTTPS URL.
#
# One URL per line (no quotes). Lines starting with # are ignored.
#
# https://api.vercel.com/v1/integrations/deploy/prj_xxxx/yyyyyyyy
# https://api.vercel.com/v1/integrations/deploy/prj_zzzz/wwwwwwww
